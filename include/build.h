#ifndef __BUILD_H__
#define __BUILD_H__

#include "gf2d_windows.h"

/**
 * @brief run the build update / do upkeep / check input
 * @return a handle to the window created
 */
Window *window_build();

#endif
